#pragma once
#include "OverworldScene.h"

// The maze: SkullBones then the Goblin, in order, behind a locked gate.
// Left edge -> back to the Forest; right edge (once cleared) -> Ruins Exterior.
class MazeScene : public OverworldScene {
public:
    MazeScene();
};
