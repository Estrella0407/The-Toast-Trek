#pragma once
#include "OverworldScene.h"

// The first overworld screen, reached from the main menu. Right edge -> Maze;
// the top-left path -> the hidden Tarumt arena.
class ForestScene : public OverworldScene {
public:
    ForestScene();
};
